language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

# command to install dependencies
install:
    - "pip install -r requirements.txt"
    - pip install pytest pytest-cov

script: 
    - "python tests.py"
    - py.test

notifications:
  slack:
    #  rooms:
    #    - ':<token>#steniq'
    on_success: always
    on_failure: always
